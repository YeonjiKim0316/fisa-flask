server {
                listen 80;

                location / {
                    include uwsgi_params;
                    uwsgi_pass unix://tmp/uwsgi.sock;
                    proxy_read_timeout 300s;
                    proxy_connect_timeout 75s;
                }
            }
      
